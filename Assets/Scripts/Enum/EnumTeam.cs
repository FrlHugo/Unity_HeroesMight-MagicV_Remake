using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnumTeam : MonoBehaviour
{
    public enum team { blue, green, red, black};
    // Start is called before the first frame update

}
